import { Section, Quote } from "reacticle";

export function SectionOpening() {
  return (
    <Section index="01" title="缓存决定一切">
      <p>
        工程界有句老话——"缓存统治一切"（Cache Rules Everything Around Me），对于 AI
        Agent 来说，这条铁律同样成立。
      </p>

      <Quote who="工程界的经典法则" source="Cache Rules Everything Around Me">
        在软件工程中，性能问题的终极答案几乎永远是"加一层缓存"。而对于长时间运行的 AI
        Agent，这个答案变成了"让缓存活下来"。
      </Quote>

      <p>
        像 Claude Code 这样长时间运行的 Agent 产品，之所以在经济上可行，完全建立在
        <strong>提示词缓存（Prompt Caching）</strong>
        之上。它让我们能够复用之前轮次的推理计算，大幅降低延迟和成本。没有缓存，就没有
        Claude Code——这不是夸张，而是经济学事实。
      </p>

      <p>
        提示词缓存的基本思想并不复杂：LLM
        在生成每个 token 时，都要"读过"当前请求中的所有上文。如果两次请求的前面一大段文字完全相同，
        API 就可以缓存住这段共享前缀的计算结果，第二次请求直接从缓存位置继续，
        而不需要从头重算。但这里有一个极易被忽视的魔鬼细节——缓存的匹配条件是
        <strong>逐字节的前缀匹配</strong>，任何差异都会导致缓存链断裂。
        这个硬约束，正是后续所有设计决策的"重力原点"。
      </p>

      <p>
        在 Claude Code 团队，我们围绕提示词缓存构建了整个运行时框架（harness）。高缓存命中率
        直接拉低使用成本，让我们有能力为订阅用户提供更宽松的调用额度——这并非锦上添花，而是
        产品的生命线。因此，我们会<strong>像监控系统可用性一样监控缓存命中率</strong>
        ：一旦命中率过低，立即触发告警，甚至直接升级为 SEV（严重事件）。
      </p>

      <p>
        这种运维姿态——把缓存断裂当作生产事故来对待——反映了缓存对 Agent
        产品经济的根本性影响。没有高缓存命中率，订阅模式根本跑不通。这也是为什么，在这篇文章中，
        我们不谈论模型能力、不谈论提示词技巧，而是聚焦于一个更底层的问题：
        <strong>如何设计 Agent 的运行时架构，让缓存活下来、活得好。</strong>
      </p>

      <p>
        以下是我们在规模化优化提示词缓存的过程中，积累下来的六条核心经验——其中不少结论
        相当反直觉，甚至与我们作为工程师的第一反应完全相反。
      </p>
    </Section>
  );
}
